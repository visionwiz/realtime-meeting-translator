# MVP版 依存関係
# 既存の音声認識システムの依存関係を継承し、新しいAPI連携機能を追加

# === 既存システムの依存関係 ===
# 音声認識（macOS特化）
mlx-lm; sys_platform == 'darwin'
mlx-whisper; sys_platform == 'darwin'

# 音声認識（クロスプラットフォーム）
openai-whisper>=20231117
torch
transformers
accelerate

# 音声処理
pyaudio>=0.2.11
sounddevice>=0.4.6
noisereduce
psutil

# === MVP版新規依存関係 ===
# Claude 3.7 Sonnet翻訳API
anthropic>=0.25.0

# Google Docs API（公式ライブラリ）
google-api-python-client>=2.170.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0
google-auth>=2.0.0

# 設定管理
python-dotenv>=1.0.0

# === 開発・デバッグ用（オプション） ===
# ログ設定強化
colorlog>=6.7.0

# パフォーマンス監視
memory-profiler>=0.60.0

# === バージョン互換性情報 ===
# Python 3.8+ 対応
# macOS: arm64/x86_64 対応
# Windows/Linux: x86_64 対応

# === インストール順序の注意 ===
# 1. 基本的な音声処理ライブラリ
# 2. Google API関連（認証設定が必要）
# 3. Claude API（APIキー設定が必要）
# 4. その他のユーティリティ

# === セキュリティ注意事項 ===
# - APIキーは環境変数または.envファイルで管理
# - credentials.jsonファイルをGitから除外
# - token.jsonファイルをGitから除外 